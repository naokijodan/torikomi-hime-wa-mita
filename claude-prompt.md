# とりこみ姫 連携テスト用プロンプト

## 基本テスト：エクスポートボタンを操作できるか確認

以下のJavaScriptをページのコンソールで実行して、とりこみ姫の「直接エクスポート」機能をトリガーしてください。

```javascript
window.postMessage({type: "TORIKOMI_EXPORT"}, "*")
```

実行後、以下を確認してください：
1. コンソールに「📡 外部からエクスポートリクエストを受信」と表示されるか
2. スプレッドシートにデータが追加されるか
3. 成功/失敗のレスポンスがコンソールに表示されるか

---

## 3つのコマンド

### 1. 直接エクスポート（スプレッドシートに送信）
```javascript
window.postMessage({type: "TORIKOMI_EXPORT"}, "*")
```

### 2. 商品データを取得（エクスポートせずにデータだけ確認）
```javascript
window.postMessage({type: "TORIKOMI_GET_DATA"}, "*")
```

### 3. エクスポートボタンをクリック（UI経由）
```javascript
window.postMessage({type: "TORIKOMI_CLICK_EXPORT"}, "*")
```

---

## レスポンスを受け取る場合

```javascript
// レスポンスリスナーを設定
window.addEventListener('message', (event) => {
  if (event.data && event.data.type === 'TORIKOMI_RESPONSE') {
    console.log('とりこみ姫レスポンス:', event.data);
  }
  if (event.data && event.data.type === 'TORIKOMI_DATA_RESPONSE') {
    console.log('商品データ:', event.data);
  }
});

// その後、コマンドを実行
window.postMessage({type: "TORIKOMI_GET_DATA"}, "*")
```

---

## Claude拡張機能への指示例

```
このメルカリの商品ページで、以下のJavaScriptを実行してください：

window.postMessage({type: "TORIKOMI_EXPORT"}, "*")

実行結果（成功したかどうか）を教えてください。
```
